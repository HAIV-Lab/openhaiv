_base_: configs/dataloader/loader.yaml

trainloader:
  dataset:
    _replace_: True
    type: remote
    root: /data/gjq/Dataset/VAIS/Fine_paired/vis
    split: train
    subset_labels: 11
    transform: train

valloader:
  dataset:
    _replace_: True
    type: remote
    root: /data/gjq/Dataset/VAIS/Fine_paired/vis
    split: test
    subset_labels: 11
    transform: test

testloader:
  dataset:
    _replace_: True
    type: remote
    root: /data/gjq/Dataset/VAIS/Fine_paired/vis
    split: test
    subset_labels: 11
    transform: test
