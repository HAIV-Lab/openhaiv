_base_: configs/dataloader/loader.yaml

trainloader:
  dataset:
    _replace_: True
    type: remote
    root: /data/datasets/CIL/imagenet-r/
    split: train
    subset_labels: 180
    transform: train

valloader:
  dataset:
    _replace_: True
    type: remote
    root: /data/datasets/CIL/imagenet-r/
    split: test
    subset_labels: 20
    transform: test

testloader:
  dataset:
    _replace_: True
    type: remote
    root: /data/datasets/CIL/imagenet-r/
    split: test
    subset_labels: 20
    transform: test
