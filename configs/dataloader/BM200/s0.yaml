_base_: configs/dataloader/loader.yaml

trainloader:
  dataset:
    _replace_: True
    type: BM200
    root: /new_data/datasets/OES_NCD/
    split: train
    subset_labels: 94
    transform: train

valloader:
  dataset:
    _replace_: True
    type: BM200
    root: /new_data/datasets/OES_NCD/
    split: test
    subset_labels: 94
    transform: test

testloader:
  dataset:
    _replace_: True
    type: BM200
    root: /new_data/datasets/OES_NCD/
    split: test
    subset_labels: 94
    transform: test