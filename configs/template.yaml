

algorithm:
  type: StandardSupervised


max_epochs: 10

criterion:
  type: CrossEntropyLoss

optimizer:
  type: Adam
  lr: 0.0003
  weight_decay: 0.0001

scheduler:
  type: Constant

device: 'cuda'


work_dir: "./work_dirs/template"
